name: mdgeomkit-test
channels:
  - conda-forge
  - defaults
dependencies:
  # Base depends
  - python
  - pip

  # MDAnalysis
  - MDAnalysis

  # additional packages
  - prettytable

  # Testing
  - pytest
  - pytest-cov
  - pytest-xdist
  - codecov
  - MDAnalysisTests

  # Pip-only installs
  #- pip:
  #  - codecov

